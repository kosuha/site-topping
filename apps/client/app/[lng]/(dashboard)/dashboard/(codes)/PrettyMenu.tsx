import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

const PrettyMenu = () => {
  return (
    <Card className="mb-8">
      <CardHeader>
        <CardTitle>Pretty Menu</CardTitle>
      </CardHeader>
      <CardContent>

      </CardContent>
    </Card>
  )
};

export default PrettyMenu;
